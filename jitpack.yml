jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.2-open
  - sdk use java 17.0.2-open
install:
  - FILE="local.properties"
  - echo "sdk.dir=$ANDROID_HOME" > $FILE
  - ./gradlew :smartalphatranslator:publishToMavenLocal
